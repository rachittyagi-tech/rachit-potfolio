# Add project screenshots here

Expected filenames (already wired in src/data/projects.ts):

    multi-agent-platform.png
    repogpt-ai.png
    creator-ai.png
    missing-person-ai.png

If a screenshot is missing, the project card shows a polished
"Project screenshot coming soon" placeholder instead of breaking.

To change a path, edit the `screenshot` field for that project in
src/data/projects.ts.
